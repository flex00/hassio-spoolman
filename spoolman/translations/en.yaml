configuration:
  log_level:
    name: Log Level
    description: "Set the (global) log level (NOTE: only enable debug when needed)."
  safe_mode:
    name: Safe mode
    description: "Start spoolman in safe mode where only the core controllers are started without any providers, to troubleshoot issues"